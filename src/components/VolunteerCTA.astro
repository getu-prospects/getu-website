---
// VolunteerCTA.astro - Dramatic full-bleed call to action for volunteers
---

<section class="volunteer-cta relative overflow-hidden">
  <!-- Clean solid dark background -->
  <div class="absolute inset-0 bg-[#1a1a1a]"></div>

  <!-- Content -->
  <div class="relative z-10 min-h-[80vh] flex items-center">
    <div class="container mx-auto px-8 md:px-16 py-24">
      <div class="max-w-3xl">
        <!-- Editorial label -->
        <div class="cta-label opacity-0">
          <span class="inline-block px-4 py-2 border border-[var(--color-secondary)] text-[var(--color-secondary)] label-editorial">
            Werde Teil der Bewegung
          </span>
        </div>

        <!-- Giant headline -->
        <h2 class="cta-headline mt-10 opacity-0">
          <span class="headline-large text-white block">
            Deine Zeit.
          </span>
          <span class="headline-large text-[var(--color-secondary)] block mt-2">
            Ihre Zukunft.
          </span>
        </h2>

        <!-- Divider -->
        <div class="cta-line mt-8 h-px w-32 bg-white/40 origin-left scale-x-0"></div>

        <!-- Description -->
        <p class="cta-text mt-10 text-xl md:text-2xl text-white/80 font-light leading-relaxed max-w-2xl opacity-0">
          Wir suchen Menschen mit Herz, die bereit sind, anderen zu helfen.
          Als Ehrenamtlicher bei GeTu Prospects kannst du direkt
          etwas bewirken und gleichzeitig selbst wachsen.
        </p>

        <!-- Benefits grid -->
        <div class="cta-benefits mt-12 grid grid-cols-1 md:grid-cols-3 gap-8 opacity-0">
          <div class="benefit-item group">
            <div class="w-16 h-16 border border-white/30 flex items-center justify-center mb-4 transition-all group-hover:border-[var(--color-secondary)] group-hover:bg-[var(--color-secondary)]/10">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"/>
              </svg>
            </div>
            <h4 class="text-white font-medium text-lg">Mentoring</h4>
            <p class="mt-2 text-white/60 text-sm">Begleite junge Menschen auf ihrem Weg</p>
          </div>

          <div class="benefit-item group">
            <div class="w-16 h-16 border border-white/30 flex items-center justify-center mb-4 transition-all group-hover:border-[var(--color-secondary)] group-hover:bg-[var(--color-secondary)]/10">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
            </div>
            <h4 class="text-white font-medium text-lg">Workshops</h4>
            <p class="mt-2 text-white/60 text-sm">Teile dein Wissen und deine Erfahrung</p>
          </div>

          <div class="benefit-item group">
            <div class="w-16 h-16 border border-white/30 flex items-center justify-center mb-4 transition-all group-hover:border-[var(--color-secondary)] group-hover:bg-[var(--color-secondary)]/10">
              <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
              </svg>
            </div>
            <h4 class="text-white font-medium text-lg">Community</h4>
            <p class="mt-2 text-white/60 text-sm">Werde Teil einer lebendigen Gemeinschaft</p>
          </div>
        </div>

        <!-- CTA buttons -->
        <div class="cta-buttons mt-16 flex flex-col sm:flex-row gap-6 opacity-0">
          <a
            href="/kontakt"
            class="group relative inline-flex items-center justify-center px-12 py-5 bg-[var(--color-secondary)] text-white font-medium text-lg overflow-hidden transition-all duration-500"
          >
            <span class="relative z-10 flex items-center">
              Ehrenamtlich engagieren
              <svg class="ml-3 w-5 h-5 transform transition-transform group-hover:translate-x-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
              </svg>
            </span>
            <div class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-500"></div>
          </a>

          <a
            href="/ueber-uns"
            class="inline-flex items-center text-white font-medium text-lg group"
          >
            Mehr erfahren
            <svg class="ml-2 w-5 h-5 transform transition-transform group-hover:translate-x-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>

</section>

<style>
  .volunteer-cta.animate .cta-label {
    animation: revealUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.2s forwards;
  }

  .volunteer-cta.animate .cta-headline {
    animation: revealUp 1.2s cubic-bezier(0.16, 1, 0.3, 1) 0.4s forwards;
  }

  .volunteer-cta.animate .cta-line {
    animation: lineExpand 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.8s forwards;
  }

  .volunteer-cta.animate .cta-text {
    animation: revealUp 1s cubic-bezier(0.16, 1, 0.3, 1) 1s forwards;
  }

  .volunteer-cta.animate .cta-benefits {
    animation: revealUp 1s cubic-bezier(0.16, 1, 0.3, 1) 1.2s forwards;
  }

  .volunteer-cta.animate .cta-buttons {
    animation: revealUp 1s cubic-bezier(0.16, 1, 0.3, 1) 1.4s forwards;
  }

  @keyframes revealUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes lineExpand {
    from { transform: scaleX(0); }
    to { transform: scaleX(1); }
  }
</style>

<script>
  function initVolunteerCTA() {
    const section = document.querySelector('.volunteer-cta');

    if (!section) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.2 }
    );

    observer.observe(section);
  }

  initVolunteerCTA();
  document.addEventListener('astro:page-load', initVolunteerCTA);
</script>
