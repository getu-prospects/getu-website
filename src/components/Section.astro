---
import Container from './Container.astro';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const _class = `!md:py-24 !py-12 ${className}`.trim();
---

<Container class={_class}>
  <slot />
</Container>
